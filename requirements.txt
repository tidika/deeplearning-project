# Core runtime dependencies
numpy==2.3.3
Pillow==11.3.0
nltk==3.9.3

# Deep learning backend
# Depending on the machine, you may need the appropriate PyTorch wheel/index.
torch==2.10.0
# Must match your torch version — install the corresponding torchvision wheel.
torchvision
